
/* Retrieves patient data associated with a unique identifier */


export const getPatient = (patientID) => {

    // call to see if patientID exists in database

    // if it does exist, return json object with patient metadata

    // if not, then return error msg
}