
/* Creates a patient, if patient does not already exist
- input: first name, last name, DOB, address, email, phone
- creates document in MongoDB under patient collection 
*/

export const createPatient = (patientData) => {

    // call to see if patient already exists in database

    // if not exists, then create a new record for the patient, return successful login page on creation


    // if does exists, return error 'Patient profile already exists'
}
